[tool.pytest.ini_options]
# Put the repo root on sys.path so `import src...` works regardless of how
# pytest is launched (console script or `python -m pytest`).
pythonpath = ["."]
testpaths = ["tests"]
